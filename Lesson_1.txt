1. установил серверную версию
2. установил gcc через команды sudo apt install gcc, также и make, perl
3.
- man открывает описание команды, введенной после него
- cd смена директоии
-- ls содержимое текущей дирекории
-- hostname имя хоста
-- whoami имя юзра
-- pwd путь к текущей папке
-- ip данные о сетевом соединении
-- sudo права суперпользователя
-- reboot перезагрузка системы
-- poweroff выкл системы
-- apt, apt-get утилита установщик
5. ввел ufw allow 22/tcp хотя и не очень понял что это значит.
ssh-keygen генератор ключей для доступа на сервер без пароля. Приватная часть ключа хранится на ПК пользователя, публичная записывается в файл authorized_keys откуда она доступна серверу для сверки с приватной частью и предоставления доступа
создал папку .ssh через mkdir в ней создал файл authorized_keys через touch через редактор nano записал туда публичную часть ключа сгенерированного путти-кеген



